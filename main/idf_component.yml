dependencies:
  espressif/esp_hosted:
    version: "==2.12.13"
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4]"
  joltwallet/littlefs:
    version: "*"
